# Retired brand assets

Superseded by the "Rise" mark in `assets/brand/` (migrated 2026-09-02).
Nothing in the app references these — they are kept only so the old identity
is recoverable without digging through git history. Safe to delete:

    git rm -r assets/legacy
